# Инструкция по деплою KiKoGame на Vercel

## ⚠️ Важное замечание

**Pygame не работает напрямую в браузере** через Pyodide или PyScript, так как требует нативного кода. Для запуска Pygame игры в браузере нужен специальный подход.

## 🚀 Варианты деплоя

### Вариант 1: Replit (Рекомендуется - самый простой)

Replit поддерживает Pygame напрямую и позволяет запускать игру в браузере **без изменений кода**.

**Шаги:**
1. Зайдите на https://replit.com
2. Создайте аккаунт (если нет)
3. Нажмите "Create Repl"
4. Выберите "Python" как язык
5. Загрузите все файлы проекта (можно через Git или загрузку файлов)
6. В консоли выполните: `pip install pygame`
7. Запустите: `python main.py`
8. Replit автоматически создаст веб-версию игры с публичной ссылкой

**Преимущества:**
- ✅ Работает без изменений кода
- ✅ Бесплатный план доступен
- ✅ Публичная ссылка для сотрудников
- ✅ Простая настройка

### Вариант 2: Vercel (Текущая настройка)

Файлы `index.html` и `vercel.json` уже созданы, но **pygame не будет работать** через Pyodide.

**Для работы на Vercel нужно:**
- Переписать игру на JavaScript/TypeScript (Phaser.js, PixiJS, или Canvas API)
- Или использовать Python сервер на Vercel (но это сложнее)

**Деплой на Vercel:**
1. Установите Vercel CLI: `npm i -g vercel`
2. В папке проекта выполните: `vercel`
3. Следуйте инструкциям
4. Или подключите GitHub репозиторий к Vercel через веб-интерфейс

### Вариант 3: Другие платформы

- **Heroku** - поддерживает Python, но требует настройки сервера
- **PythonAnywhere** - бесплатный хостинг для Python приложений
- **Streamlit Cloud** - если переписать на Streamlit (но это изменит интерфейс)

## 📋 Текущие файлы проекта

- ✅ `index.html` - веб-интерфейс с Pyodide
- ✅ `vercel.json` - конфигурация для Vercel
- ✅ `package.json` - метаданные проекта
- ✅ `.gitignore` - исключения для Git

## 💡 Рекомендация

Для вашего случая (быстрый деплой без изменений кода) **используйте Replit**:
- Самый простой способ
- Работает "из коробки"
- Публичная ссылка для сотрудников
- Бесплатный план достаточен

## 🔧 Если нужно использовать именно Vercel

Тогда потребуется переписать игру на JavaScript. Я могу помочь с этим, но это займет время и изменит код проекта.

